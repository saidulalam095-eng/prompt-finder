# Prompt Finder V2
Free AI image prompt discovery website. No payment system.
Run: npm install && npm start
Open http://localhost:3000
For public launch, deploy on a Node-compatible host. Add stronger production moderation, rate limiting, abuse reporting and secure data practices before accepting large-scale user submissions.
